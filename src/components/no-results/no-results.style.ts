import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: { alignItems: "center", marginTop: 40 },
  image: { width: 240, height: 240, opacity: 0.85 },
  text: { color: "#888" },
});
